{
  "name": "notarial-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Notarial - Property transfer on Scroll",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network scroll",
    "deploy:testnet": "hardhat run scripts/deploy.js --network scrollSepolia",
    "verify": "hardhat verify --network scroll",
    "seed": "hardhat run scripts/seed-demo.js --network scroll"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0"
  }
}
